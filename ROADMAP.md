# MongoDB Log & Metric Analyzer Roadmap

## 当前阶段

新版离线 LogVis 页面已实现并验证，包含图表／数据表切换、指标说明、右侧日志解读、独立模式最慢样本，以及追加的三位小数、面板尺寸记忆、长模式提示和本地任务删除。

已完成表头排序及一次前后端全面审查修复，页面已收敛装饰、增强数据对齐和异常披露。

FTDC 指标分析已实现、完成真实数据验收并完成异常文件与查询内存加固。应用顶部可切换 MongoDB Log／MongoDB Metric；FTDC 采用原始文件加二进制索引、有界多指标组图和重型操作严格串行，页面支持指标组多选、核心组快捷选择、逐组增量渲染、单组失败隔离及图表批量控制，发布最大堆为 `-Xmx2g`。

MongoDB 4.4＋日志运行诊断与 Markdown 报告导出已完成并通过真实日志验收。现有慢查询 `summary.json`、接口、默认页面和统计口径保持不变；新增诊断使用独立持久化与按需加载。

使用方法与限制已在 Web 顶部常驻入口和独立说明文档中补齐，覆盖 Log／Metric 操作、数据保留、资源上限与分析结论边界。仓库现提供中英文项目介绍和中英文详细用户指南，方便中文及英文使用者快速了解功能、启动方式、使用步骤、隐私策略与能力边界。

项目已完成独立设计整理与开源迁移：设计文档不再使用其他项目作为设计依据，Java 命名空间统一为 `com.whaleal.mongodblog`，代码以 Apache License 2.0 开源并发布到 `whaleal-dev/mongodb-log`。

## 已完成

- 已确认产品定位：运维人员个人电脑离线使用的本地 Web 小工具。
- 已移除设计文档、实施计划、测试命名和进度记录中的其他项目名称、本地源码路径与外部实现依据，明确本项目的独立设计规则。
- 已将 Maven `groupId` 与 Java 主代码、测试、实施文档路径从旧命名空间统一迁移到 `com.whaleal`。
- 已添加 Apache License 2.0，以全新单提交 Git 历史公开发布到 `whaleal-dev/mongodb-log`，未带入旧远端、旧分支或旧提交。
- 已确认数据保留策略：每次上传一个任务，永久保存全局耗时最长的 Top 5000 条慢查询，并独立保留 Top 50 查询模式各自最慢的一条日志。
- 已确认耗时分布口径：基于全部慢查询，使用固定耗时区间精确统计。
- 已完成日志格式、统计口径和页面交互需求梳理，形成本项目的独立设计。
- 已建立项目级开发规范和目录约定。
- 已实现 MongoDB 结构化 JSON、旧版文本与 GZip 日志流式解析。
- 已实现基于全部慢查询的八档耗时分布，以及耗时最长 Top 5000 精确保留。
- 已实现本地任务存储、单线程异步分析、查询筛选和 REST API。
- 已实现日志上传、任务历史、分析进度、统计图表、查询模式和明细抽屉。
- 已按新版区块顺序完成任务首页与结果页，补齐全量聚合、CPU 区间、小时连接数和日志时间范围。
- 已实现 macOS、Linux、Windows 启动脚本和浏览器自动打开。
- 已生成可执行 JAR，并完成 Java 17 下的实际启动验证。
- 已使用 312,065,070 字节真实日志完成端到端验收。
- 已将用户提供的 Web 页面方案整理为独立需求文档，并核对区块、字段、交互、统计口径及文档引用。
- 已实现统计图／数据表互斥切换、中文指标及问号说明，保留 HeartBeat Failed 等数据库术语。
- 已实现 Namespace 散点缩放、右侧详情与基于实际日志的本地解读，独立样本不受 Top 5000 限制。
- 已实现小数四舍五入到 3 位，长查询模式悬浮或聚焦展示全文。
- 已实现 13 个指标面板拖动宽高、浏览器本地布局记忆、恢复默认与窄窗口约束。
- 已实现完成／失败任务删除与对应文件清理，活动任务拒绝删除，保护原始日志和其他任务，清理失败明确报错。
- 已实现 Top 50 内文本／数值列点击升降序排序、方向标记和缺失值置后。
- 已修复任务切换、重叠轮询及删除后旧列表响应的竞态；结果页显示解析失败、部分解析与跳过行数。
- 已修复 checkpoint 心跳误计、findAndModify 误识别、update／getMore 模式遗漏、非法数值污染及旧版数值溢出。
- 已修复重启后的排队任务卡住，以及上传副本未清理就发布完成状态的问题。
- 已优化标题层级、边框、留白和数值对齐，并限制图表最小高度，保持拖动、说明和精确读数功能。
- 已建立项目的 Git 版本管理，`main` 分支包含源码、文档、启动脚本与测试。
- 已完成 FTDC 指标分析设计：原始文件落盘、二进制 block 索引、指标组有界图表查询及全局串行内存控制。
- 已完成 FTDC 分阶段实施计划，明确测试驱动顺序、真实 10 MB 样本和 20 文件 `-Xmx2g` 验收标准。
- 已实现 FTDC 外层 BSON 流式读取、Block 有界解压、类型展平、uvarint／RLE 解码和损坏数据拒绝。
- 已实现原始 FTDC 本地保留、紧凑二进制索引、源文件 SHA-256 校验、独立任务恢复与安全删除。
- 已实现公平串行许可、跨文件时间合并、重叠点上传顺序优先和指标组有界降采样。
- 已实现 MongoDB Log／MongoDB Metric 顶部切换及浏览器本地选择记忆、FTDC 多文件上传、任务进度与概览、指标组筛选、原始值／相邻差值切换。
- 已在右上角实现每 5 秒刷新一次的 JVM 堆内存圆形指示器；点击后二次确认，可清空全部 Log／Metric 终态任务及应用托管数据，存在活动任务时在删除前整体拒绝。
- 已将 Web 页面及前端错误提示中的 FTDC 用户可见文案统一为 Metric，底层 FTDC 技术模型与接口路径保持不变。
- 已按本项目定义的父路径与特殊前缀规则实现 401 个真实指标组；页面支持多选指标组并按选择顺序串行查询，支持一键选择当前任务实际存在的核心指标组，以及组内多指标合并展示／分开展示和折叠。
- 已实现多指标组逐组返回、逐组渲染，后续组加载时已完成组立即可见；图表区支持一键展开／关闭全部组，以及一键隐藏／显示全部全零指标。
- 已实现分组 Block 单次解压和多列恢复；单组最多 200 个指标、每指标最多 1,200 点。
- 已实现全零指标一键隐藏／恢复，并按主要采样间隔插入空点，使时间断裂处不连线且 Tooltip 显示 `-`。
- 已移除产品层单指标模式及 catalog／series／values／export 公开接口，只保留内部有界列解码能力。
- 已按 MongoDB 官方 FTDC 解码约束增加单 Block 10,000,000 字节和「指标数 × 样本数」1,000,000 上限，并额外限制单 Block 100,000 个样本；所有规模校验均在主要数组分配和解码循环前完成。
- 已改为按声明长度定长解压并严格拒绝长度不符、截断、压缩尾部、非法 varint 和越界 RLE；异常文件返回明确格式错误，不再尝试容错解码。
- 已实现指标组选择性索引、同源文件通道复用、请求内元数据复用和逐 Block 流式恢复，避免为无关指标创建列级数组或物化全部原始序列。
- 已实现选择顺序严格串行、单组错误卡片与后续组继续加载、最终失败数汇总，以及任务轮询轻量防重叠标记。
- 已在完整原始时间流上使用过滤后相邻间隔中位数判断时间断裂，降采样为真实断裂保留 `null` 并显示 `-`；桶内最小值／最大值按时间输出并保留首尾点，完整扫描继续负责全零及 `min`、`max`、`avg` 统计。
- 已将 macOS／Linux／Windows 发布启动链路统一为 `-Xms128m -Xmx2g`，并补齐 FTDC 数据、隐私、磁盘和内存说明。
- 已实现独立 `diagnostics.json`，流式聚合严重级别、组件、异常事件时间线、连接与认证、客户端应用／Driver、复制集／网络事件、慢查询效率线索及数据可信度；诊断保存失败不改变原有 `summary.json` 完成语义。
- 已按字段能力兼容 MongoDB 4.4～8.1 的 `queryHash`、`planCacheShapeHash`、`queryShapeHash`、Query Framework、CPU、分片／鉴权／队列／Oplog 等待和执行阶段落盘字段，缺失字段与零值分开统计。
- 已在日志结果页增加“慢查询分析／运行诊断”页签，原慢查询页保持默认且组件和布局 key 不变；诊断首次进入时按需读取。
- 已实现当前任务一键导出脱敏 Markdown 报告，包含任务、数据质量、异常、连接、复制集、慢查询效率、规范化模式和分析边界；历史任务缺少诊断文件时可降级导出。
- 已在 Web 顶部新增“使用说明”弹窗，并新增独立用户文档，统一说明启动方式、Log／Metric 操作步骤、本地数据保留、上传与查询上限以及非实时监控、非自动根因判断等边界。
- 已补齐英文项目介绍与英文详细用户指南，并在中英文 README、用户指南之间加入语言切换和返回入口；同步修正中文 README 中已过时的 Metric 单指标组操作描述。

## 进行中

- 无。

## 待办

- 无。

## 阻塞

- 无。

## 最近验证

- 2026-09-16：中英文项目介绍与用户指南完成，并按实际代码逐项核对解析格式、上传与查询上限、任务调度、数据保留、报告脱敏规则、API 边界、启动参数及 Web 界面按钮；修正结构化日志版本表述、未知字段覆盖率、Metric 多指标组流程和报告隐私边界。后端 107 项测试零失败、2 项真实样本按环境条件跳过；前端 49 项测试通过，前端生产构建与 Maven 打包成功，文档相对链接目标及 `git diff --check` 检查通过。
- 2026-09-16：独立设计与开源迁移完成；旧组织标识、其他项目名称、本地源码路径及对应实现依据扫描无匹配。后端 107 项测试零失败、2 项真实样本按环境条件跳过；前端 49 项测试通过；前端生产构建与 Maven 打包成功，可执行 JAR 已生成。
- 2026-09-11：Web 使用说明与独立用户文档完成；前端 49 项测试通过，后端 107 项测试零失败、2 项真实样本按环境条件跳过，前端生产构建和 Maven 打包成功。浏览器在窄窗口确认顶部入口、弹窗内容滚动和 Escape 关闭正常，控制台零错误、零警告。
- 2026-09-11：复审 Markdown 报告并修正并行开发暴露的问题：旧任务不再把历史 Top 20／执行计划当作新版全量口径，补齐日志文件、Top 50 独立最慢样本、诊断概览、异常事件脱敏样本和复制集类型，修复 CPU 表头 Markdown 转义、无 CPU 数据空表以及 IPv6／用户标识脱敏。后端 106 项测试零失败、2 项按环境条件跳过，前端 48 项测试通过，Maven 打包成功。两份真实日志导出的报告为 849 行、59,578 字节，包含 15 个查询模式及其最慢样本、37 条 Top 5000 明细、13 条异常事件样本、223 个诊断时间桶和 37 条诊断明细；敏感值扫描无匹配。
- 2026-09-11：Markdown 报告按 Web 的“慢查询分析／运行诊断”两个页签补齐相关数据，新增完整聚合列、Top 20、全量 Namespace 响应量、CPU 分布、每小时连接数、Top 50、脱敏 Top 5000、异常时间桶、Query Framework 和完整诊断明细。后端 103 项测试零失败、2 项按环境条件跳过，前端 48 项测试通过，Maven 打包成功。使用两份 312,065,070 字节真实日志导出的报告为 796 行、53,818 字节，包含全部 37 条已保留慢查询和 223 个诊断时间桶；完整 IP、MongoDB URI、密码／token 关键词及测试命令字面值扫描无匹配。
- 2026-09-11：FTDC 异常文件与指标组查询加固完成并复审。复审修复了根据降采样后不规则极值时间误判断裂的问题，改为在完整原始时间流上识别真实断裂，并在点数预算内与首尾、尖峰、低谷一起输出；轮询防重叠标记改为按任务 ID 隔离。后端 107 项测试零失败、2 项按环境条件跳过，前端 48 项测试通过，前端生产构建与 Maven 打包成功。10,485,242 字节真实样本在 `-Xms64m -Xmx256m` 下完成 96 个 Block、19 个核心指标组和 20 文件回归，GC 记录的堆使用峰值约 122 MiB、回收后约 49～51 MiB，无 OOM。浏览器确认 19 个核心组、187 个指标全部显示，无失败卡片，控制台零错误、零警告。
- 2026-09-11：MongoDB Log 运行诊断和 Markdown 报告完成；后端 99 项测试零失败、2 项按环境条件跳过，前端 45 项测试通过，生产构建和 Maven 打包成功。使用两份共 312,065,070 字节、936,570 行的真实日志完成分析，结果仍为 37 条慢查询；诊断识别 26,655 条异常级别日志、173,377／173,364 次连接建立／结束、156 次复制集相关事件和 37 条慢查询线索。浏览器确认原慢查询页默认显示、运行诊断按需加载、4.4＋字段覆盖可见，并实际点击导出得到成功提示；报告 198 行、16,806 字节，敏感值规则扫描无匹配。
- 2026-09-11：内存圆球与全量清理完成；隔离数据目录验证 Log／Metric 任务目录和索引实际删除，活动任务验证零删除；浏览器确认内存从 31.060 MB 刷新到 33.954 MB，点击出现取消／确认清空二次提示并取消，现有 2 个 Metric 测试任务保持不变，页面用户可见文案统一为 Metric。前端 43 项测试通过；后端 95 项测试零失败、2 项按环境条件跳过；生产构建及 Maven 打包通过。
- 2026-09-11：顶部 MongoDB Log／MongoDB Metric 选择已持久化；自动测试覆盖两种选择重新挂载后的恢复，浏览器分别选择 Metric 和 Log 后刷新均进入对应工作区，最后恢复为 Metric。前端 41 项测试、生产构建及 Maven 打包通过；后端 89 项测试零失败、2 项按环境条件跳过。
- 2026-09-11：多组查询改为串行增量渲染，并补齐全局展开／关闭、隐藏／显示全零控制；前端 41 项测试、后端 89 项测试（2 项真实样本测试按环境条件跳过）、生产构建及 Maven 打包通过。浏览器以 19 个核心组验证加载到 10／19 时前 10 个组已显示，全部加载后全局关闭／展开 19 个组均生效，控制台零错误、零警告；同一真实样本查询期间 Java 进程 RSS 峰值 241.8 MiB、结束后 178.2 MiB，JVM 堆峰值及结束值 85.9 MiB。
- 2026-09-11：指标组多选与核心组快捷选择完成；前端 40 项测试、后端 89 项测试（2 项真实样本测试按环境条件跳过）、生产构建及 Maven 打包通过。浏览器使用真实 10 MB FTDC 样本一键选中 19 个核心指标组、187 个指标，顺序查询后渲染 19 个组卡片和 19 张合并图，控制台零错误、零警告。
- 2026-09-11：FTDC 页面收口为单组查询，修复一次渲染 401 个组以及组卡片未占满父容器导致 ECharts 不可见的问题；浏览器确认 `network` 组合并图生成 1 个画布，隐藏 2 个全零指标后分开展示生成 5 个画布。
- 2026-09-11：后端 89 项测试通过，真实样本与 20 文件验收 2 项单独启用后通过；前端 39 项测试、生产构建和 Maven 打包通过，单指标公开接口返回 404。
- 2026-09-11：多指标分组功能加入后，后端 89 项测试通过（真实样本 2 项另行启用并通过），前端 40 项测试和生产构建通过，Maven 打包成功。
- 2026-09-11：真实 FTDC 样本的 `network` 组完整返回 `bytesIn`、`bytesOut` 等 7 个指标，每指标不超过 1,200 点；20 文件建索引及单指标查询回归继续通过。
- 2026-09-11：浏览器一次查询 13 个 `network` 相关指标组，确认纵向分组、7 指标合并图、7 张拆分图、折叠／展开和点击指标进入单指标详情均正常；控制台零错误、零警告。
- 2026-09-11：最终后端 87 项测试通过，其中真实样本 2 项在独立命令中启用并通过；前端 39 项测试和生产构建通过，Maven 打包成功生成最新 JAR。
- 2026-09-11：用户提供的 10,485,242 字节 FTDC 样本确认 96 个 Block、2,082 个指标和 28,800 个点；`start`、`serverStatus/connections/current`、`serverStatus/network/bytesIn` 前 10 点与预先固化的验收向量逐值一致。
- 2026-09-11：20 份真实样本在 `-Xms128m -Xmx2g` 下完成 1,920 个 Block、576,000 个原始点的建索引、2,000 点图表、1,000 点分页和完整流式导出；GC 记录末尾老年代维持 13 个 region，没有随文件数持续增长。
- 2026-09-11：最终 JAR 浏览器验收完成顶部切换、真实文件上传、进度、42 页指标目录、搜索、图表、差值、288 页原始值和 CSV.gz 下载；控制台零错误、零警告。删除确认提示已核对并取消，实际删除由集成测试覆盖。
- 2026-09-11：FTDC 开发开始；已先更新项目规范，明确原始 FTDC、二进制索引、单指标有界查询和全局公平串行许可。
- 2026-09-11：按确认结果将 FTDC Java 最大堆和压力验收上限从 `-Xmx256m` 调整为 `-Xmx2g`；分块、单指标、串行和禁止全量物化的设计不变。
- 2026-09-11：只读核对现有任务、存储、接口、前端和项目规范，完成 FTDC 设计规格与实施计划；本轮仅修改文档，未修改业务代码，未运行功能测试。
- 2026-09-10：首次推送前复跑后端 66 项、前端 35 项测试全部通过；当前文件及历史提交的凭据模式扫描无匹配，依赖、构建产物和日志数据未纳入提交。推送后核对 GitHub main 与本地代码提交 356aa1c 一致。
- 2026-09-10：全面审查后，后端 66 项、前端 35 项测试全部通过，mvn package 成功；前后端修复复审无新增 P1／P2。
- 2026-09-10：浏览器合成日志验收确认平均耗时升序 50／200／1000、降序 1000／200／50，691 字符查询模式全文可见；600px 窗口无整页横向溢出，控制台无错误或警告。
- 2026-09-10：合成日志中无效 JSON 与负数耗时分别显示失败／部分解析，checkpoint 恢复不计入 HeartBeat Failed；新版 JAR 已更新到本机 18081。
- 已有历史统计不会自动重算，重新上传后应用修正后的解析口径。上传保存失败时的尽力清理与多进程共享数据目录未纳入本轮验证。

- 2026-09-10：新版后端 55 项、前端 28 项测试通过；mvn package 成功，包含最新前端静态资源。
- 2026-09-10：合成数据 5103 条慢查询，全局保留 5000；Top 50 的 20ms 独立样本不在全局 Top 5000 仍可查看。数据保留、样本并列排序及历史结果兼容均已验证。
- 2026-09-10：浏览器确认面板拖动宽高后刷新恢复，图表随尺寸调整，600px 窗口无整页横向溢出，三位小数和模式提示正确，控制台无错误或警告。
- 2026-09-10：删除集成测试覆盖实际文件清理、原始文件／其他任务隔离、重启持久化、活动任务拒绝、符号链接保护和失败重试；浏览器确认删除提示和取消有效，没有删除用户真实任务。
- 2026-09-10：新版 JAR 已启动在本机 18081 验收端口。代码复审发现的 BOM 样本解释问题已修复，最终复审无 P1／P2。

- 2026-09-10：完成新版 Web 需求文档与设计、实施计划的自洽性检查；本轮仅改文档，未修改应用代码或运行功能测试。
- 2026-09-10：确认项目初始为空目录。
- 2026-09-10：完成项目初始 Git 仓库与 `main` 分支准备。
- 2026-09-10：确认本机存在 Java 17、Maven 3.9.16、Node.js 26 和 npm 11。
- 2026-09-10：后端 36 个测试全部通过；前端 5 个测试全部通过。
- 2026-09-10：mvn package 成功生成 target/mongodb-log-analyzer.jar，并确认静态页面已打入 JAR。
- 2026-09-10：使用两份共 312,065,070 字节的真实日志验证，936,570 行全部完成解析。
- 2026-09-10：真实日志中的 Slow query 原文计数与程序结果均为 37 条；Top 结果最大耗时 23,497 ms，八档分布合计 37 条。
- 2026-09-10：在 Java 17 下完成 JAR 启动、上传、汇总、Top 排序、筛选明细和详情抽屉端到端检查。
- 2026-09-10：实机检查 LogVis 风格任务首页和任务详情页，浏览器控制台无错误或警告。
